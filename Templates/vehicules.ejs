<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Véhicules</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
    <div class="container mx-auto p-6">
        <h1 class="text-3xl font-bold text-center mb-6">Liste des Véhicules</h1>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <% vehicules.forEach(vehicule => { %>
                <div class="bg-white shadow-md rounded-lg p-4">
                    <h2 class="text-xl font-semibold mb-2"><%= vehicule.marque %> <%= vehicule.modele %></h2>
                    <p class="text-gray-700">Année : <%= vehicule.annee %></p>
                    <p class="text-gray-700">Prix : <%= vehicule.prix_par_jour %> €</p>
                    <p class="text-gray-700">Kilométrage : <%= vehicule.immatriculation %> km</p>
                    <form action="/vehicules/<%= vehicule.id %>" method="DELETE" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce véhicule ?');">
                        <button type="submit" class="bg-red-500 text-white px-4 py-2 rounded hover:bg-red-600">Supprimer</button>
                    </form>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>